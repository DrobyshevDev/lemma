# Everything needed to build the site and run every notebook in the course.
# Deliberately small: a reader who cannot install the course cannot take it.

mkdocs-material>=9.7.7
mkdocs-static-i18n>=1.3.1      # the English build is one file per page, not a fork

# Диапазон намеренно широкий: читатель приходит с тем NumPy, который у него уже
# стоит. Поэтому код ноутбуков не должен звать ничего, что различается между
# первой и второй мажорной версией -- np.trapz, np.float_, np.product и прочее
# убранное. CI ставит самую свежую версию и ловит такие вызовы.
numpy>=2.5.1
matplotlib>=3.11.1

# Executing the notebooks in CI, which is how we know they still run.
jupyter>=1.1.1
nbconvert>=7.17.1
nbformat>=5.10.4
ipykernel>=7.3.0
